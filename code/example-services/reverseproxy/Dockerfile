# Use the official Caddy image on Alpine Linux for a small footprint.
FROM caddy:latest

# Copy your custom Caddyfile configuration into the container.
# This replaces the default Caddy configuration.
COPY Caddyfile /etc/caddy/Caddyfile